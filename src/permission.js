import router from './router'

const role = 'admin'

// router.beforeEach((to, from, next) => {
//     if (to.meta.roles.includes(role)) {
//         next()
//     } else {
//         next({path: '/404'})
//     }
// })
